services:
  frigate:
    # Expose the Frigate web UI only for local development and troubleshooting.
    ports:
      - "127.0.0.1:5000:5000"

  vyzio-api:
    environment:
      ASPNETCORE_ENVIRONMENT: Development
