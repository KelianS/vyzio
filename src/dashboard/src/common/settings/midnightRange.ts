/** One sentence for any time range that runs past midnight, so every screen says it alike. */
export const midnightRangeHint = (end: string) =>
  `La plage passe minuit : elle se termine le lendemain à ${end}.`
