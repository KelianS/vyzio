dist/
node_modules/
# Generated by pnpm — reformatting it produces a huge diff and no benefit.
pnpm-lock.yaml

# Vendored shadcn/ui primitives (ADR-42) — kept byte-identical to upstream so that
# re-running the shadcn CLI produces no formatting noise, and an update stays reviewable.
src/common/ui/
