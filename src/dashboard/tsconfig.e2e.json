{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.e2e.tsbuildinfo",
    /* DOM because code passed to `page.evaluate` runs in the browser. */
    "lib": ["ES2023", "DOM"],
    "types": ["node"]
  },
  "include": ["playwright.config.ts", "tests"]
}
